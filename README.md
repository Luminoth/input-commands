# Input Context / Commands Example

TODO: need to map out the flow of InputManager (client) runs current InputContext to push whatever to InputState (sync'd to server) and *thing* that makes use of the that (server). *thing* is what runs the commands.
